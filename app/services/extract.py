

async def extract_text_driver(driver, x_path):
    """Extract Text from a given XPATH

    Args:
        driver (WebDriver): Selenium WebDriver
        x_path (str): XPATH to extract text from

    Returns:
        str: Text extracted from the XPATH
    """
    return ""
